# Python
__pycache__/
*.pyc

# Secrets — never commit (EODHD token + Gmail SMTP creds)
config.env

# Local run artifacts
logs/
*.log

# Backtest / search CSV outputs (regenerable)
*.csv

# ETF price data — refreshed from EODHD at runtime
json/

# Screenshots / images
*.png
